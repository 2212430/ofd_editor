server:
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  web:
    resources:
      add-mappings: false

logging:
  level:
    com.ofdeditor: DEBUG